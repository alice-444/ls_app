FROM node:23-alpine

WORKDIR /app

COPY ./project .

RUN corepack enable pnpm
ENV PNPM_HOME="/pnpm"
ENV PATH="$PNPM_HOME:$PATH"

RUN chmod +x /app/dev.sh

EXPOSE 3000

ENTRYPOINT ["sh", "/app/dev.sh"]